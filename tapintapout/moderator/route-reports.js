// Simple client-side search filter for the routes table

document.addEventListener('DOMContentLoaded', function () {
    const table = document.querySelector('.routes-table');
    if (!table) return;

    // Optional: add search later if you want
    // For now this file is a placeholder for future JS features.
});
